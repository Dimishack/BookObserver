<Window x:Class="BookObserver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookObserver"
        xmlns:vm="clr-namespace:BookObserver.ViewModels"
        xmlns:us="clr-namespace:BookObserver.Views.UserControl"
        Title="{Binding Title}" Height="480" MinHeight="480" Width="800" MinWidth="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Window.InputBindings>
        <KeyBinding Modifiers="Ctrl" Key="Q" Command="{Binding CloseWindowCommand}"/>
    </Window.InputBindings>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Файл"/>
            <MenuItem Header="Правка">
                <MenuItem Header="Настройки"/>
            </MenuItem>
        </Menu>
        <TabControl>
            <TabItem Header="Книги">
                <us:BooksUserControl DataContext="{Binding Source={StaticResource Locator}, Path=BooksUC}"/>
            </TabItem>
            <TabItem Header="Читатели"/>
        </TabControl>
    </DockPanel>
</Window>
